# Configs

Configuration files for prompts, style guides, and workflow definitions.

- `prompts/planner.md`: planning-layer prompt that defines the unified workflow rule.
- `workflows/v1.json`: the shared workflow definition that planning should instantiate for each paper.
